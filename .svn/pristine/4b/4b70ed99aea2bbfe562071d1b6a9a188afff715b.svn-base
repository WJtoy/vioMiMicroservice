package com.kkl.kklplus.b2b.viomi.utils;

import com.kkl.kklplus.b2b.viomi.http.response.FaultType;
import com.kkl.kklplus.b2b.viomi.http.response.FaultTypeLevel;

import java.util.ArrayList;
import java.util.List;

public class TreeUtils {


    //将list集合转成树形结构的list集合
  /*  public static List<FaultType> listToTree(List<FaultType> list) {
        List<FaultType> treeList = new ArrayList<FaultType>();
        for (FaultType tree : list) {
            if (tree.getData() != null) {
                treeList.add(findChildren(tree, list));
            }
        }
        return treeList;
    }*/

    //寻找子节点
  /*  private static FaultType findChildren(FaultTypeLevel tree, List<FaultType> list) {
        for (FaultType node : list) {
            if (node.getData() != null) {
                if (tree.getData() == null) {
                    tree.setData(new ArrayList<FaultTypeLevel>());
                }
                tree.getData().add(findChildren(node, list));
            }
        }
        return tree;
    }*/

}
